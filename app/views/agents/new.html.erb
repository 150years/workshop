<% content_for :title, "New agent" %>

<h1 class="font-bold text-4xl mbe-4">New agent</h1>

<%= render "form", agent: @agent %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Back to agents", agents_path, class: "btn" %>
</div>
