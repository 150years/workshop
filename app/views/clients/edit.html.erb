<% content_for :title, "Editing client" %>

<h1 class="font-bold text-4xl mbe-4">Editing client</h1>

<%= render "form", client: @client %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Show this client", @client, class: "btn" %>
  <%= link_to "Back to clients", clients_path, class: "btn" %>
</div>
