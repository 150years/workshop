<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= content_for?(:title) ? yield(:title) : "Document" %></title>
    <style>
      body { font-family: "Helvetica"; font-size: 12px; }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
