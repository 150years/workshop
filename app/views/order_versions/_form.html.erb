  <%= form_with(model: order_version, url: order_version.persisted? ? order_version_path(@order, order_version) : order_versions_path(@order), html: { contents: true }) do |form| %>
  <% if order_version.errors.any? %>
    <div class="alert alert--negative flex flex-col gap-half mbe-4" role="alert">
      <h2 class="font-medium leading-none"><%= pluralize(order_version.errors.count, "error") %> prohibited this order_version from being saved:</h2>
      <ul class="text-sm mis-3" style="list-style: disc">
        <% order_version.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="flex flex-col items-start gap-half mbe-4">
    <%= form.label :profit, "Profit, %", class: "text-sm font-medium leading-none" %>
    <%= form.text_field :profit, class: "input" %>
  </div>
  <div class="flex flex-col items-start gap-half mbe-4">
    <%= form.label :agent_comm, "Agent, %", class: "text-sm font-medium leading-none" %>
    <%= form.text_field :agent_comm, class: "input" %>
  </div>
  <div class="flex flex-col items-start gap-half mbe-4">
    <%= form.label :version_note, class: "text-sm font-medium leading-none" %>
    <%= form.text_field :version_note, class: "input" %>
  </div>
  <div class="flex flex-col items-start gap-half mbe-4">
    <%= form.label :note_for_client, class: "text-sm font-medium leading-none" %>
    <%= form.text_field :note_for_client, class: "input" %>
    <p style="color: var(--color-negative);">Client will see this note!</p>
  </div>
  <div class="flex items-center gap  mbe-4">
    <%= form.check_box :final_version, class: "checkbox" %>
    <%= form.label :final_version, class: "text-sm font-medium" %>
  </div>
  <div class="inline-flex items-center justify-between mbs-2 mie-1 gap">
    <%= link_to "Back", @order, class: "btn" %>
    <%= button_tag tag.span(order_version.persisted? ? "Update Version" : "Create Version"), class: "btn btn--primary btn--loading" %>
  </div>
<% end %>
