<% content_for :title, "New order version" %>

<h1 class="font-bold text-4xl mbe-4">New order version</h1>

<%= render "form", order_version: @order_version %>

<div class="inline-flex flex-wrap items-center gap mbs-2" style="--row-gap: .5rem">
  <%= link_to "Back to order", @order, class: "btn" %>
</div>
