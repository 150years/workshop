<% content_for :title, "Editing product" %>
<%= turbo_frame_tag @product, target: "_top" do %>
  <h1 class="font-bold text-4xl mbe-4">Editing product</h1>
  <%= render "form", product: @product %>
<% end %>
